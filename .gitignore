# Runtime data files — never commit real financial data.
# See README.md "Data and privacy".
split-*.json
split-session-*.json
split-*-archive.json

# Local source and reference material — never publish or treat as a Git backup.
/resources/
/design-reference/

# Experimental web application dependencies, builds, local state, and credentials.
/webapp/node_modules/
/webapp/dist/
/webapp/.wrangler/
/webapp/.env*
!/webapp/.env.example

# OS / editor cruft
.DS_Store
